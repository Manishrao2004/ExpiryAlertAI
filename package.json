{
  "name": "expiry-alert-ai",
  "version": "1.0.0",
  "description": "ExpiryAlert AI – Smart Expiry Detection & Tracking System",
  "private": true,
  "scripts": {
    "setup": "cd backend && npm install && node scripts/generateIcons.js && cd ../frontend && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "start:backend": "cd backend && npm start",
    "build:frontend": "cd frontend && npm run build"
  }
}
